{
  "name": "proximity-credit-repair",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Proximity Credit Repair — React + Vite frontend with Express API proxy for Gemini, Firebase Client SDK for auth and database.",
  "scripts": {
    "dev": "npm run dev --prefix client",
    "build": "npm run build --prefix client",
    "preview": "npm run preview --prefix client",
    "install:all": "npm install && npm install --prefix client",
    "lint": "npm run lint --prefix client",
    "typecheck": "npm run typecheck --prefix client"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "express": "^4.19.2",
    "vite": "^5.4.21",
    "@vitejs/plugin-react": "^4.7.0"
  },
  "devDependencies": {
    "firebase-tools": "^13.0.0"
  }
}
