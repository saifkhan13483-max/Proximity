{
  "name": "proximity-credit-repair",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Proximity Credit Repair — React + Vite frontend with Firebase Auth, Firestore, and Google Gemini AI.",
  "scripts": {
    "dev": "npm run dev --prefix client",
    "build": "npm run build --prefix client",
    "preview": "npm run preview --prefix client",
    "install:all": "npm install --prefix client",
    "lint": "npm run lint --prefix client",
    "typecheck": "npm run typecheck --prefix client"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
